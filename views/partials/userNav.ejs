<nav class="navbar navbar-expand-lg sticky-top   navbar-fixed-top navbar-dark bg-dark p-0 py-3">
  <div class="container">
    <a class="navbar-brand"
      href='<%=(currentUser && currentUser.type === "lecteur" )?"/user/1":"/documents/all/all/1"%>'>
      Home
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <li class="nav-item px-2">
          <a class="nav-link" href="/documents/all/all/1">Parcourir les documents</a>
        </li>
        <!-- <li class="nav-item px-4">
          <section id="search_bar" class="my px-5">
            <div class="container">
              <form action="/documents/all/all/1" method="POST">
                <div class="row justify-content-around">
                  <div class="col-md-4 p-1">
                    <select name="filter" class="form-control rounded-pill">
                      <option selected disabled>Select Filter...</option>
                      <option name="titre">Titre</option>
                      <option name="auteur">Auteur</option>
                      <option name="categorie">Categorie</option>
                      <option name="ISBN">ISBN</option>
                    </select>
                  </div>

                  <div class="col-lg-8 m-0 p-1">
                    <input
                      name="searchName"
                      type="text"
                      class="form-control rounded-pill"
                      placeholder="Search Documents"
                    />
                  </div>

                  <div class="col-md-2 p-1 px-6 m-0 justify-content-around">
                    <input
                      type="submit"
                      class="btn rounded-pill btn-outline-primary"
                      value="Search"
                    />
                  </div>
                </div>
              </form>
            </div>
          </section>
        </li> -->
      </ul>
      <%if(currentUser && currentUser.type === 'lecteur') { %>
      <ul class="navbar-nav bg-dark ml-auto text white">
        <%if(currentUser.violationFlag) { %>
        <li class="nav-item align-self-center mr-2 text-white">
          <a disabled class="btn-danger btn btn-sm rounded-pill"><i class="fa fa-flag text"></i> Suspendu </a>
        </li>
        <% } %>
        <li class="nav-item dropdown mr-3">
          <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
            <!-- <i class="bi bi-person-circle"></i>  -->
            <img src="<%=(currentUser.image !==
            "")?`/images/${currentUser.image}`:`/images/profile.png`%>" width="20" /> <%=currentUser.username%>
          </a>
          <div class="dropdown-menu bg-dark py-3 text-white">
            <a href="/user/1/profile" class="dropdown-item text-white">
              <i class="fa fa-user-circle"></i> Profile
            </a>

            <a href="/auth/user-logout" class="dropdown-item text-white">
              <i class="bi bi-person-x"></i> Logout
            </a>
          </div>
        </li>
      </ul>
      <% }else { if(!currentUser) {%>

      <a href="/auth/user-Login" class="btn rounded-pill btn-primary btn px-4 ml-auto">Login</a>

      <% } }%>
    </div>
  </div>
</nav>