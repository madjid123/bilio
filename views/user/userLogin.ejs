<% include ../partials/header %> <% if(currentUser && currentUser.type !==
'lecteur') {%> <% include ../partials/adminNav%> <%} else {%> <% include
../partials/userNav %> <%}%> <% include ../partials/alerts%> <% include
../partials/alerts %>

<section id="userLoginForm">
  <div class="container mt-5">
    <div class="row">
      <div class="col-md-6 offset-md-3">
        <div class="card">
          <div class="rounded-card-header text-center">
            <h4>User Login</h4>
          </div>
          <div class="card-block justify-content-center">
            <form class="p-3" action="/auth/user-login" method="POST">
              <div class="form-group">
                <label class="form-label">Username</label>
                <input
                  type="text"
                  name="username"
                  placeholder="Username"
                  class="form-control rounded-pill"
                />
              </div>
              <div class="form-group">
                <label class="form-label">Password</label>
                <input
                  type="password"
                  name="password"
                  placeholder="Password"
                  class="form-control rounded-pill"
                />
              </div>
              <small class="py-1 my-2"
                >Vous n'avez pas un compte ?
                <a href="/auth/user-signUp/"> créer un compte</a></small
              >
              <button
                type="submit"
                class="btn rounded-pill px-5 btn-primary btn-block"
              >
                Login!
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<% include ../partials/footer %>
